package MinimumSpanningTree;

public class Prim {

	/*
	 * (G,r)
	 * for each u in G.V
	 * 		u.key = LARGE
	 * 		u.parent = null
	 * r.key = 0
	 * Q = G.V
	 * while(Q is not Empty)
	 * 		u = getMinimum(Q)
	 * 		for each v in G.Adj[u]
	 * 			if( v in Q and w(u,v)<v.key)
	 * 				v.parent = u
	 * 				e.key = w(u,v)
	 * 
	 * */
	
}
